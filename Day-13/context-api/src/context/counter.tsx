import { Children, createContext } from "react";
import { useState } from "react";
export const CounterContext = createContext(0)

export const CounterProovidor=({Children}:any)=>{
    const [counter,setCounter]=useState(0);
    return (
        <
    )
}